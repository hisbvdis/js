// =============================================================================
// Входящие данные
// =============================================================================
const data = {...}



// =============================================================================
// Рекурсивная функция
// =============================================================================
function func(n) {
  // 1. Простейший случай
  // 1.1. Если: ...
  // 1.2. Вернуть: ...
  if (n === 0) return 0;
  
  // 2. Сложный случай
  // 2.1. Если: все остальные случаи
  // 2.1. Вызвать функцию, передав...
  // 2.2. Вернуть: результат вызова функции...
  return func(n - 1);
}



// =============================================================================
// Вызов функции
// =============================================================================
console.log( func(1) );